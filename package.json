{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.1",
    "mongodb": "^5.7.0",
    "mongoose": "^7.3.4",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": "16.16.0"
  },
  "scripts": {
    "build": "cd client && yarn build",
    "install-client": "yarn && yarn build",
    "heroku-postbuild": "yarn run install-client && yarn run build",
    "start": "node server.js",
    "start-watch": "nodemon server.js",
    "dev": "concurrently \"yarn run start-watch\" \"cd client && yarn start\""
  }
}
